/*
const fs = require('fs');

fs.writeFileSync('notes.txt', 'I AM GROOT');

fs.appendFileSync('notes.txt', '  STARLORD!');
*/

const add = require('./utils');

const sum = add(4, 3);

console.log(sum);
